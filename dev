#!/usr/bin/env node
// Copyright (c) 2026 bachbnt

const { main } = require('./src/cli');

main(process.argv.slice(2)).then(
  (exitCode) => {
    process.exitCode = exitCode;
  },
  (error) => {
    console.error(`DevCmd failed: ${error.message}`);
    process.exitCode = 1;
  }
);
