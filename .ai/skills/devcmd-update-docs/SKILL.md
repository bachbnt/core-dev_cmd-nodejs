---
name: devcmd-update-docs
description: Synchronize DevCmd README, CLI help, completion, examples, installation instructions, and release documentation with actual behavior. Use after user-facing command changes, framework additions, option changes, installation or release updates, and documentation audits.
---

<!-- Copyright (c) 2026 bachbnt -->

# Update DevCmd Documentation

Derive documentation from implemented behavior and verified commands, not from assumptions.

## Workflow

1. Read `src/cli.js`, `src/config/index.js`, `src/recipes/built-ins.json`, `src/commands/completion.js`, argument parsing, and the relevant handlers.
2. Compare framework names, lifecycle actions, options, defaults, aliases, prerequisites, and examples across help, completion, and README.
3. Run commands with `--dry-run` before documenting generated command behavior.
4. Verify installation URLs, repository metadata, release links, the current version or tag, and `npm pack --dry-run` contents when referenced.
5. Separate end-user installation from contributor setup. Make clear whether a click downloads an archive or actually installs software.
6. Keep examples copy-pasteable and avoid platform claims that were not tested.
7. Add or update tests when help or completion content is generated by code.

## Documentation Rules

- Prefer version-independent links such as `/releases/latest` where appropriate.
- Pin a specific tag only for reproducible installation examples.
- State optional SDK requirements next to the feature that needs them.
- Do not claim npm registry installation until the package is published there. The registry name `devcmd` currently belongs to an unrelated project, so GitHub installation examples must use an explicit `bachbnt/dev-cmd` URL.
- Distinguish the latest tagged release from the latest committed source on `main`.
- State whether repository-only contributor files, including `.ai/skills`, are excluded from the installed package.

## Verify

Run documented non-destructive commands, `npm test`, link/string checks where useful, and `git diff --check`.
